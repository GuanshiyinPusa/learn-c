// write ANSI C function headings for the following functions described. Note
// that you need write only the headings, not the body.
// a. n_to_char() takes an int argument and returns a char.
// b. digits() takes a double argument and an int arument and returns an int.
// c. which() takes two addresses of double as arguments and returns the address
// of a double
// d. random() takes no argument and returns an int

char n_to_char(int);
int digits(double, int);
double which(double *, double *);
int random(void);
