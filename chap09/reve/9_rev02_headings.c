// Write ANSI C function headings for the following functions described. Note we
// are asking just for the headings, not the body a. donut() takes an int
// argument and prints that number of 0s b. gear() takes two int arguments and
// return type int. c. guess() takes no arguments and returns an int value d.
// stuff_it() takes a double and the address of a double variable and stores the
// first value i nthe fiven location

// a.
void donut(int);
int gear(int, int);
int guess(void);
void stuff_int(double, double *);
